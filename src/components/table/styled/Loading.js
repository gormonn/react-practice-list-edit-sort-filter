// const LoadingWrap = styled.div`
//   position: absolute;
//   top: 0px;
//   left: 0px;
//   transition: opacity 1s;
//   width: 100%;
//   height: 100%;
//   background: rgba(255, 255, 255, 0.5);
//   display: "block";
//   /* display: ${({ loading }) => (loading ? "block" : "none")}; */
//   opacity: ${({ isLoading }) => (isLoading ? 1 : 0)};
//   /* opacity: 0; */
// `;
// function Loading({ isLoading }) {
//   const className = isLoading ? "is-loading" : "not-loading";
//   return (
//     <div
//       className={`loading loading__${isLoading ? "is-loading" : "not-loading"}`}
//     ></div>
//   );
// }
